# 项目结构说明

本项目已按照 Vue 官方规范进行重构，目录结构如下：

## 📁 目录结构

```
waf_front/
├── public/                    # 静态资源目录
│   ├── maps/                 # 地图数据文件
│   └── reports/              # 报告文件
├── src/
│   ├── api/                  # API 接口模块
│   │   ├── alertAPI.js       # 告警管理 API
│   │   ├── authAPI.js        # 认证 API
│   │   ├── client.js         # API 客户端（带鉴权）
│   │   ├── index.js          # 流量分析 API
│   │   ├── report.js         # 报告 API
│   │   ├── rulesAPI.js       # 规则管理 API
│   │   └── securityAPI.js   # 安全态势 API
│   │
│   ├── assets/               # 静态资源
│   │   └── world.json        # 世界地图数据
│   │
│   ├── components/           # 可复用组件
│   │   └── dashboard/        # 图表组件
│   │       ├── AttackTrendChart.vue    # 攻击趋势图表
│   │       ├── AttackTypePie.vue        # 攻击类型饼图
│   │       └── BlackWhiteTrend.vue     # 黑白名单趋势图
│   │
│   ├── composables/          # 组合式函数（Composables）
│   │   ├── useAuth.js        # 认证相关逻辑
│   │   └── useDashboard.js  # 仪表板数据管理
│   │
│   ├── router/               # 路由配置
│   │   └── index.js          # 路由定义
│   │
│   ├── utils/                # 工具函数
│   │   ├── china.js          # 中国地图注册工具
│   │   └── loadChinaMap.js   # 地图加载工具
│   │
│   ├── views/                # 页面级组件（Views）
│   │   ├── Dashboard.vue            # 主仪表板（路由容器）
│   │   ├── LoginPage.vue             # 登录页面
│   │   ├── TrafficAnalysis.vue       # 流量分析页面
│   │   ├── SecurityStatus.vue        # 安全态势页面
│   │   ├── ProtectionReport.vue      # 防护报告页面
│   │   ├── WafDashboard.vue         # 监控大屏页面
│   │   ├── AlertManagement.vue      # 告警管理页面
│   │   └── RuleManagement.vue       # 规则配置页面
│   │
│   ├── App.vue               # 根组件
│   ├── main.js               # 应用入口
│   └── style.css             # 全局样式
│
└── package.json              # 项目配置
```

## 📋 目录说明

### `/src/api/` - API 接口模块
- 所有 API 相关代码集中管理
- 使用统一的 API 客户端（`client.js`）处理鉴权和请求

### `/src/components/` - 可复用组件
- 存放可在多个页面复用的组件
- `dashboard/` 子目录存放图表类组件

### `/src/composables/` - 组合式函数
- 使用 Vue 3 Composition API 的逻辑复用
- 遵循 Vue 官方命名规范（`use` 前缀）

### `/src/views/` - 页面级组件
- 存放路由对应的页面组件
- 每个页面组件对应一个路由路径

### `/src/utils/` - 工具函数
- 存放通用的工具函数和辅助方法
- 不依赖 Vue 的纯函数

### `/src/router/` - 路由配置
- 集中管理所有路由定义
- 包含路由守卫逻辑

## 🎯 命名规范

### 组件命名
- **页面组件（Views）**: 使用 PascalCase，如 `TrafficAnalysis.vue`
- **可复用组件（Components）**: 使用 PascalCase，如 `AttackTrendChart.vue`
- **组合式函数（Composables）**: 使用 camelCase，以 `use` 开头，如 `useAuth.js`

### 文件命名
- Vue 组件文件：PascalCase，如 `LoginPage.vue`
- JavaScript 工具文件：camelCase，如 `loadChinaMap.js`
- API 文件：camelCase，如 `authAPI.js`

## 🔄 导入路径规范

### 页面组件导入
```javascript
// 在 views/ 目录内
import TrafficAnalysis from './TrafficAnalysis.vue'

// 从其他目录导入页面
import LoginPage from '../views/LoginPage.vue'
```

### 组件导入
```javascript
// 从 components/ 导入
import AttackTrendChart from '../components/dashboard/AttackTrendChart.vue'
```

### 工具函数导入
```javascript
// 从 utils/ 导入
import { loadChinaMap } from '../utils/loadChinaMap.js'
```

### API 导入
```javascript
// 从 api/ 导入
import { trafficAPI } from '../api/index.js'
import { securityAPI } from '../api/securityAPI.js'
```

## ✅ 已完成的优化

1. ✅ 创建 `utils/` 目录，将 `lib/` 目录下的工具函数迁移
2. ✅ 将页面级组件从 `components/` 移到 `views/`
3. ✅ 更新所有导入路径
4. ✅ 删除未使用的文件（`Globe.vue`、`data/` 目录、`lib/` 目录）
5. ✅ 更新路由配置

## 📝 注意事项

- 所有页面级组件现在位于 `views/` 目录
- 可复用组件保留在 `components/` 目录
- 工具函数统一放在 `utils/` 目录
- 遵循 Vue 官方推荐的目录结构规范

